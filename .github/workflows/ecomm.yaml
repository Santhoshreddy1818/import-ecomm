name: ecomm.yaml 
on: push
jobs:
  ecomm-workflow:
   runs-on: self-hosted
   steps: 
   - name: apt-get updating
     run: sudo apt-get update
   - name: nginx installing
     run: sudo apt-get install nginx -y
   - name: checking ports
     run: ss -ntpl
    
